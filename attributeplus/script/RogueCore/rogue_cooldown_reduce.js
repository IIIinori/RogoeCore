// RogueCore AttributePlus script. Deploy to plugins/AttributePlus/script/RogueCore/rogue_cooldown_reduce.js
var priority = -1
var combatPower = 0.4
var attributeName = "冷却缩减"
var attributeType = "OTHER"
var placeholder = "rogue_cooldown_reduce"

function onLoad(attr) {
  return attr
}

function run(attr, entity, handle) {
  return false
}
