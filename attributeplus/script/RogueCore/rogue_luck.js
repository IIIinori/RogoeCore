// RogueCore AttributePlus script. This attribute is registered for RogueCore systems to read.
var priority = -1
var combatPower = 0.4
var attributeName = "幸运"
var attributeType = "OTHER"
var placeholder = "rogue_luck"

function onLoad(attr) {
  return attr
}

function run(attr, entity, handle) {
  return false
}
